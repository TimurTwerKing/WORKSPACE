// id number, name string, city string, photo string, wifi boolean, meters number.

export interface HousingLocation {
    id: number;
    name: string;
    city: string;
    photo: string;
    wifi: boolean;
    meters: number;
};